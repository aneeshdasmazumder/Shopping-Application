spring.cloud.config.uri=http://localhost:8888

#Not adding this will effect @RefreshScope to work
management.endpoints.web.exposure.include=*

#spring.data.mongodb.uri=mongodb://localhost:27017/product-service

#Eureka Register Name
spring.application.name=product-service

#To dynamically assign free port at the time of startup since we will have multiple instances
server.port=0

#To uniquely identify each instance
eureka.instance.instance-id=${spring.application.name}:${random.uuid}

spring.cloud.vault.host=localhost
spring.cloud.vault.port=8200
spring.cloud.vault.scheme=http
spring.cloud.vault.authentication=TOKEN
spring.cloud.vault.token=s.VSWboaMYMWteneidWU6mBkFk
spring.cloud.vault.application-name=product-service

spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest